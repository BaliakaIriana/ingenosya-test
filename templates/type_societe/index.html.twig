{% extends 'base.html.twig' %}

{% block title %}TypeSociete index{% endblock %}

{% block body %}
    <h1>TypeSociete index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Designation</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for type_societe in type_societes %}
            <tr>
                <td>{{ type_societe.id }}</td>
                <td>{{ type_societe.designation }}</td>
                <td>
                    <a href="{{ path('type_societe_show', {'id': type_societe.id}) }}">show</a>
                    <a href="{{ path('type_societe_edit', {'id': type_societe.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="3">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('type_societe_new') }}">Create new</a>
{% endblock %}
